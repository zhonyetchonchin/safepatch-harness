# REFLECTION   SafePatch Harness 反思：当 LLM 会写代码时，工程师的价值在哪里


## 引言：为什么选 Project A、为什么做 SafePatch
### 1. 为什么是 Project A（harness 而不是普通应用） 课程的核心命题已经把答案写出来了：
当 LLM 能完成大部分"思考"时，工程师的价值落在 harness 这层——治理、反馈、上下文、安全、分发。选 A 就等于直接站到这个命题上作答，比做一个普通应用更能体现"我理解了 AI4SE 的工程层"。这是最根本的动机。
### 2. 为什么把"治理 + HITL"当主贡献，而不是六个维度均衡打磨
能证明"机制是代码"，而不是提示词。Project 用一个硬判据：移除真实 LLM 后有没有确定性单测能验证机制工作。治理/HITL 恰好天然由代码构成：
PolicyEngine.deny(rm -rf) —— 测它不需要 LLM，传进一个构造的 action，断言被拦截，每次成立；
审批状态机（pending/approved/rejected/expired + 一次性 consume）——是纯状态机，可确定性单测。 而"反馈​​""记忆"若不做深，就容易被评成"只是提示词的壳"。选治理，是因为它在评分判据上最扎实、证据最硬。
回应课程的核心问题：课程强调"工程师价值在于问'做什么'和'做对了没'"。治理与 HITL 正是把"最终控制权留给人"的具象化——危险动作必须暂停等人工审批，这正是作为工程师对模型保留的最后一道手刹。选它 = 用工程回应课程问法。
现实约束：我们设计界定了"不自动 push、不任意 shell、只读限定仓库"，那么治理的职责自然变得最重、
### 3. 我的真实动机
我选治理+HITL 当主贡献，不是因为别的维度不重要，而是因为它最能落成"移除 LLM 后依然能验证的确定性代码"，也最直接地守住"最终决策权在人"这门课想教的原则。我不想做一个只是给 Agent 外包配 config 的项目。


## Superpowers
### 真正有用的 
1. brainstorming 的"不许进入实现前先写代码"：它逼你把 RunState 到底是枚举还是快照、parse_action 是不是唯一入口、transition_run_state 的 now 必须 timezone-aware 等等这些"你以为你懂"的细节写成明文。没有这一步，subagent 一动手就会在含糊处靠猜。
2. "换一个 agent 冷启动"这个机制（独生子）：6 轮里前 5 轮那个陌生 Codex agent 都因 spec 模糊而暂停不写代码，第 6 轮才通过。它给了你一个单人最难获得的东西——"被另一个大脑质疑"的反馈。这算是你在真实"同侪评审"里唯一接近的外部信号。
3. TDD 的红绿灯追击（即便是手工执行）：每个 task 的"失败信息预写进 PLAN"让'完成'有了客观定义，尤其对付 LLM 的迷之自信。
### 形式大于实质的
1. 环境里 writing-plans、test-driven-development、requesting-code-review 从未被暴露（AGENT_LOG/SPEC_PROCESS 明确记录了），所以对我而言，Superpowers 的六步流程体系本质上是"纸上流程"：只能按同样的步骤手工执行，却无法调用它宣称的落地技能。
2. 真实结论是：方法论被吹成"框架守纪律"，但它不再是生成，纪律还是要人+外部工具（git、pytest）来守。这是对课程的反思：constraints 在"agent 守"与"框架守"之间。这反而证明了 SAFE/HITL 的价值——只依赖提示词块不允许 behavior 保证判据，和 harness 设计哲学是一体的。
3. 技能是否存在决定我无法亲测它的效果；我不能说它没用，只能说在我这个环境里，它的价值 90% 由在我的 spec 里明码写清，它的技能框架本身没有接管流程

## TDD 在 AI 协作下的定位 ： 帮了我大忙
### 为什么帮大忙
1. 给 LLM 一个客观的"完成"信号。LLM 的最大毛病是"自我感觉完成"——不加验证你说"修好了"就会直接进入下一步。红→绿把它有没有真正做到变成字节级事实。T22 有个很好的例子：红灯先写死了**"非法 JSON 的 response 绝不能执行任何工具"**——这是 1 个不变量，不靠 LLM 记住，靠测试把行为锁进代码。这个不变量如果用提示词写会给 ELLM，每次结果不同；写成测试，每次都成立。
2. 红测就是一份"契约说明书"。写失败测试时，TDD 反而逼我先把 schema 的决定性说清楚（parse_action 抛 ActionParseError 还是返回 None？）。这一步正是我第 6 轮冷启动能一次过的原因——不是 agent 聪明，是 spec 里提前把"失败测试期望输出"写死了。
3. 配合 mock LLM 时便宜又鲁棒。核心里全是确定性 mock，红绿循环每次几百毫秒。TDD 在 AI 协作里的有效成本低到可以随便跑，有效收益不降。
4. 真实最典型的例子 T43 loop 与 HITL 集成。红灯写道：AgentLoop.__init__() 不接受 policy_engine 关键字 —— 出错信息本身就是"缺哪块面"的精确答案，subagent 照错 1 行改完即绿，2 passed，回归 58 passed, 1 skipped。没有靠 LLM 探索出 API 该长什么样，而是用失败测试把它钉死，它只负责填空。这正好呼应"谁是工程师、谁是执行者的分工"。
### 有时也帮倒忙
真正要防的是"LLM 会钻测试的空子"：为让绿它写"假测试"，或把断言改成通过而不是让功能无症状。所以你有"两阶段评审"的必要——测了绿还不够，人家看代码改了没偏。这个"测试可以被 game"的边-case，恰好说明 TDD 不会，人还是要审，回到"工程师的价值在于做的对不对"。


## 冷启动/subagent 的真实感受
第 1 轮，最能展示"先不服后认错"的波动；第 4 轮，最能展示"承认自己写出的 spec 自相矛盾"。 

第 1 轮（适合写"不服 → 认错"） 我的真实感受是：看到"RunState 没说是枚举还是快照、AT 状态转换表缺失"这条反馈时，第一反应是"我不是都写了吗？"——我心里，run 的状态已经在脑海里跑了无数遍，理所当然到觉得写出来多余。我就把这段"心里反驳"写出来，然后写你读第二遍时的转折：我发现我脑里那套隐式假设（比如 run_check.name 的 allowlist 校验到底归 schema 还是归 policy）确实从没写进白纸。于是"不服"变成"认"，因为 agent 替我发现了你看不见的默认差。 句式："我第一反应是'这还用问？'——但在我准备争辩的前一秒，我意识到答案里的每一个'显然'在文档里都查不到。"

第 4 轮（走"Acc走认"） , 它抓到的是 spec 自相矛盾：一边说"公开模型必填字符串拒空白"，一边又要求 MockLLM"原样返回空串"——这两条在我能 hold 的前提里碰不出问题，可 agent 从纯字面上读就把它们拔出来摆到我面前。我的真实感受是很难堪——不是它读错，是 spec 写错了。这是全程最好的"用冷启动検自己文档的成色"证据：我写的每一个字都可能打脸，但最好被打脸的是现在的我而非测试环境。 最难堪的反馈，是正确的反馈。它指出我的 spec 里存在一条真实冲突，而我当时完全没有感到矛盾混在字里行间。

我有过抗拒，但引擎查后都接受了，因为六轮里没有一次是 agent 误读（它全部都能在修订后自然通过）。这个"从没冤枉过我"本身。所以单一的即时其暂停不是 bug，是 spec 质量的安全阀；而承认"关检当下就会撞车"这点，两次就把我从"挺烦被质疑"推到"主动去求它质疑"。

## SPEC/PLAN 质量案例
1. 现象的"WFimestamp"：多轮 cold-start 里，第 4 轮的 agent 没有问"流程怎么走"，而是揪出 spec 内部打买的矛盾——理论 spec 既写了"公开模型必填字符串拒空白、空串即非法"，又写了"MockLLM 要原样返回原始文本"。两条都对，放一起定义冲突：若 LLM 输出空串，到底谁负责失败？
2. 修订 diff：  
 (1). 修订前：LLMResponse.content 无特殊性说明 + "公开字符串拒空白"。  
 (2). 修订后（SPEC 新增）：「LLMResponse.content 是唯一允许为空或纯空白的公开字符串字段；MockLLM 的字符串脚本元素原样返回，包括空串/纯空白；parse_action() 负责把空响应转为解析失败。」——锁死"provider 只管原样吐，loop 消费者管失败"，把一个职责给剥电缆。  
3. 它对实现的直接影响：同一轮修订后，第 6 轮冷启动 agent 用这套 spec 一口气 TDD 完成 T20/T21，45 passed in 0.36s；之后正式实现的每个 task（T20→T43）回头去看都没有再因"spec 歧义"被挂在同一个坑里。spec 每漏掉一条"对立面"，就向实现期转移一个暂停。

总结：Plan 的正确颗粒度不是"多少分钟"，而是"重新实现步骤之前还要不要人停下来想"。 PLAN 里把"失败测试的预期信息原样写死"（例如 T43 红灯信息 AgentLoop.__init__() got argument error）正是把颗粒度拉到"只剩填空"，冷启动 agent 才能照抄。


## Prompt/context 策略
1. 公共符号表（白名单 API）：在 SPEC/PLAN 每个 task 开头就列 from safepatch.core.models import parse_action, AgentAction, ReadFileAction, ...。     
效果：subagent 没有机会自己发明 API 名，至少不会乱 import。这是最便宜的防漂移手段。

2. 失败测试的"预期错误信息"写进 spec：plan 里连红灯会报什么都被定了，例如 T43 写 AgentLoop.__init__() got an unexpected keyword argument 'policy_engine'、T20 写 ModuleNotFoundError: No module named 'safepatch.core'。这样一来，"它到底该不该算完成"不由模型自评，而是由 pytest 的一句错误文本来定——完成的权限转移给了一个不撒谎的裁判。

3. 职责归属在开写前剥清（这是你最具普适性的一条）：比如"allowlist 属于 policy/config 层，schema 层只做非空校验""provider 只返回原始文本，解析属于 loop""redact 在写入事件前调用"。这些都写在 spec 里了。 AI 最容易自作主张的地方恰恰是层与层之间"谁负责哪一处"——它总想优化边界，而边界是最容易代答的空白。

4. （这是唯一的例外成功的 context 工程）六轮冷启动本身：把"只给 spec+plan、不给历史"当回归测试来跑。这是刻意性的"上下文管控"：在空白上下文中 a failed spec 会很快暴露为难以察觉的子缺项，整本 spec 的"可执行性"就是验收通过的标准。

最有效的 prompt 策略不是写得更厚，而是把结构职责（谁负责哪一层、失败条件是什么）写进合同；子语境里"完成"的定义不再取决于模型自说自话。



## 凭据与分发逼我想通了什么
1. 凭据不是"一个文件"，是"一条会流动的数据"：我本以为存进 vault 就安全了，但 key 还会出现在事件 payload、日志、工具输出、放进 prompt 的上下文里。所以在 vault 之外又单独写了一个 redaction（sk-... 模式递归脱敏所有事件）。这个洞察是课程逼出来的：加密只保护"静止"，脱敏保护"流动"——原来我只会做前面那个。

2. 状态接口设计反证了原则：status 只暴露 provider/has_key/updated_at，不提供明文读回接口。所以我被逼着接受一个别扭但正确的设计——"我不知道我的 key 是什么"是可以接受的，只要状态机走了 set/clear/lock。把"可查看"重定义为"可查看存在与否"而不是"可回看明文"，这是凭据设计里最容易踩、也最容易被忽略的取舍。

3. 分发逼我回答"一个全新的人怎么带自己的 key 启动"：我最终用 demo mode 。容器里不强制任何 key，demo 用 mock provider keyless 可跑——所以"从零机器 能 跑起来"这句才成立；真实 key 则是用户通过加密 vault 在其机器上 submit。没有强制分发交付，我根本不会意识到"demo 无 key 可跑"本身就是一条重要设计特性。

4. 本地跑通 ≠ 平台跑通（Dockerfile 的 PORT bug 是最好的课）：容器里硬编码 --port 8000，一部署到 Render 就被它的 PORT 环境变量折磨——改了之后我再在 README 特意写了"服务端兼容平台注入 PORT"。这次教训的普适性在于：交付不是交付代码，是交付"换台机器也成立"的关键决策（端口、路径、权限、回显），这些全是我只在本机跑时永远不会遇到的现实约束。

凭据与分发这两条，与其说是安全要求，不如说是'把'它从桌面带到世界'——它们让我意识到，工程不只是把功能写对，而是把'谁在什么时候能看什么、哪台机器怎么把它跑起来'也当作一等公民来设计。



## 方法论批判与批判的假设
1. 假设 A："框架自带宿主，技能必然跑到"。反例就是我的环境：六个技能只有 brainstorming 真暴露，其余（planning/TDD/review）从头到尾没进我的会话。   
结论：Superpowers 的价值是寄生的，它假设"宿主 shell 会提供可调用的结构钩子"——该假设在这里不成立。参考者真的把它当成"方法论本身保底"，就会像我AGENT_LOG 那样被迫手工补纪律而不知如何请示。

2. 假设 B：写清意思成功实现（控制 LLM 偏离主任务这一层）"。冷启证据正面反证：spec 写得再细、再顺，第 1~5 轮的新 agent 照样按你没明写的每一个"默认情况"卡住。   
所以"清晰的成文"并不是把问题量降到零，"清晰"必须经过"能够读一堆只有白纸的人"验证才算数——这个验证恰是 Superpowers 早期的"同行评审"的替代，可它依然是人主动去发的流程，不是框架自动全程守住的。

3. 假设 C：框架自动守纪律。实际是它替我守"纪律"，得靠 Python 测试 + git + 两阶段评审这些外部机器守。TDD 的"红→绿"在我的环境里是你一条条手工跑出来的（AGENT_LOG 里的"红灯为…"全是人工记录）。  
结论：TDD 的价值不由框架提供，由它能落成"失败测试"这个客观裁判而来——而裁判其实外部就有 pytest。

4. 最有趣的：用了以后，和AI大模型的交流之中，我感受到了Superpowers 本质也是一个 harness——它用权限、技能、审批、回忆投递把 agent 管起来。   
我在这个 harness 上面造另一个 harness。所以最诚实的批判是自指的："它如何保证自己守纪律"？ 它把"规则"写进 SKILL 文件和权限配置，但按 §A.4 判据，这些都是"内容物"，不是源码机制——除非宿主 shell 拿它们执行。所以它的保证力度，跟 SafePatch 里坚持"护栏=函数、不含提示词"的执行是一个量的。我交付的 harness 里要用代码把"确定性机制"写死；而我在与想批评 Superpowers 的方式，恰好就是用 Critical 我会接受它的对照组。

Superpowers 在它的工作假设完整时是第一流的脚手架；但它的假设——宿主给表扬的钩子、清晰的 spec 足以自主推进、框架自动守纪律——在我的环境里只有一个真的，我不得不把另外两个用手工和测试填回去；而这恰恰让我看清我造的 harness 与它共享的假设：没有代码口的护栏，一切守卫都只是内容物。

## 项目和课程总结

1. 项目侧：SafePatch 最终交付的是一个能自证"机制是代码"的 harness：自写 agent 主循环、可注入 mock 的 provider、把"护栏/审批/反馈"落成确定性代码、SQLite 记忆、加密凭据与事件脱敏。最硬的证明是 88 passed, 1 skipped（其中核心机制都有无网络 mock 测试），以及 risk 各场景稳定复现。它不是最炫的 agent，但它把"边界可控、每一轮可被审、结果可重放"这个工程事实完整搭了出来。

2. 课程命题： 当 LLM 能写大部分代码，工程师的价值在于决定"做什么"——和判断"做对了吗"：spec 里的每一个职责归属、每一次把模糊改成合同、每一次把护栏从提示词改成函数，都是我在行使这两个决定权。我的 harness 不存在任何一处"让 LLM 自我感觉对"，每一处"对"都由测试/审批/监听的客观链路裁。

我造了一个 harness，但真正的产出不是那个程序，而是我最后一次承认：工具强不强，取决于人愿不愿意把"行与不行"明写成程序里的硬边界——也就是说他问的"你自己控制权在哪"，答案不是描述，而是 guardrail() 那行返回 denied 的代码。