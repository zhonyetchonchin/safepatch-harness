stages:
  - test

unit-test:
  image: python:3.12-slim
  stage: test
  before_script:
    - python -m pip install --upgrade pip
  script:
    - python -m pip install -e ".[dev]"
    - python -m pytest -q
