# REFLECTION · SafePatch Harness：当 LLM 会写代码时，工程师的价值在哪里

> 本文由学生本人构思；AI 辅助压缩字数与润色。
## 引言：为什么选 Project A，为什么选治理 + HITL

课程命题已经把答案写出来了：当 LLM 能完成大部分"思考"时，工程师的价值落在 harness 层——治理、反馈、上下文、安全、分发。我选 Project A 是直接对着这道命题作答，不想做一个只给 Agent 包一层 config 的项目。

我把主要贡献放在"治理 + HITL"，理由是它最能落成"机制是代码，不是提示词"：课程用"移除真实 LLM 后能否用确定性单测验证"作硬判据，而 PolicyEngine 拦截（如 `deny(rm -rf)`）、审批状态机（pending/approve/reject/expired + 一次性消费）恰好天然是可测的纯函数；治理同时把"最终控制权留给人"，直接回应课程最想问的"谁来决定做什么、做对了没有"。

## Superpowers：哪些真有用，哪些形式大于实质

真正有用的两件事。一是 brainstorming 逼我在写代码前先把细节写成明文：`RunState` 是枚举还是快照、`now` 必须 timezone-aware、`parse_action` 是不是唯一入口、Pydantic 是否拒绝多余字段。没有这一步，subagent 一动手就会在含糊处凭猜。二是"换一个 agent 冷启动"：六轮冷启动里，陌生 agent 前五轮都因 spec 有歧义而暂停不写代码。它给了单人项目最接近"同侪评审"的外部信号。

形式大于实质的是：我环境里只有 brainstorming 一个 skill 真正被暴露，`writing-plans`、`test-driven-development`、`requesting-code-review` 从头到尾没被调用。六步流程我只能手工模仿，纪律最后还是靠 git、pytest 和人补回来的——框架在我这边并没有接管流程。

## TDD 在 AI 协作下的定位：助大过于阻

TDD 给 LLM 一个客观的"完成"定义，把"做没做到"变成字节级事实，专治模型的"自我感觉完成"。T22 把"非法 JSON 绝不能执行工具"写成红灯不变量；T43 红灯信息写死"`AgentLoop.__init__()` 不接受 `policy_engine`"，错误文本本身就是"缺哪块"的答案，subagent 按错改一行即绿。配合确定性 mock，每轮红绿几百毫秒，便宜又可反复跑。

它也有帮倒忙的边界：LLM 可能钻测试空子、写假断言"让测试变绿而不是让功能正确"，所以我保留两阶段评审——绿还不够，人要看它有没有改偏方向。这恰好回到"工程师的价值在于判断对错"。

## 冷启动：从"不服"到"主动求它质疑"

最真实的一段在第 1 轮：看到"`RunState` 没说明是枚举还是快照"时，我第一反应是"这些还要写吗"，准备反驳的瞬间才发现——我心里那套隐式假设（比如 allowlist 校验到底归 schema，靠 policy 归谁）从没写进过白纸。于是"不服"变成"认"。

第 4 轮最难受：spec 一面写"公开模型必填字符串拒绝空白"，一面又写"MockLLM 需原样返回空串"，新 agent 从字面把这条自相矛盾摆到我面前。不是它读错，是我的 spec 真写错了。六轮里没有任何一次是它误读，这让我从"烦被质疑"变成"主动求它质疑"——专心暂停不是 bug，是 spec 质量的安全阀。

## SPEC/PLAN 质量如何影响实现

上述空串冲突是最典型的例子。我的修订是：明确"provider 只管原样返回，`parse_action()` 负责把空响应转为失败"，把困惑的职责归属锁死在文档里。修订后第 6 轮 cold-agent 用这套 spec 一口气 TDD 完成 T20/T21，45 个测试、0.36 秒全绿；之后正式实现再没卡在"spec 歧义"上。

我的结论：PLAN 的最优颗粒度不是"若干分钟"，而是"重新实现之前还要不要人停下来想"。把每个 task 的失败测试预期信息（含错误文本）写进 PLAN，颗粒度就小到了 subagent"只剩填空"，冷启动才能一次过。

## Prompt / context 策略

有效的三招：① 在 SPEC/PLAN 开头固定每个 task 的公开导入符号，subagent 没有机会发明 API 名；② 把红灯的预期错误信息写进 plan，让"完成与否"由 pytest 这个不撒谎的裁判裁决；③ 开工前把层间职责剥清（allowlist 属于 policy/config，解析属于 loop，脱敏在写入事件前），AI 最容易自作主张的地方恰是层与层之间。另外，"只给 spec+plan、不给历史"的冷启动本身就是最狠的语境压测——把 spec 的"可执行性"当成验收标准。

## 凭据与分发逼我实录了什么

四件事我原本都不会想到：一，**凭据是"流动"的**——加密只保护静止的 vault，事件/日志里流动的 key 需要另写一套递归脱敏（`sk-...`）机制；二，状态接口不提供明文回读、只回 `has_key` 是"故意别扭"，把"查看"重定义为"查看是否存在"，这是设计师要过的取舍；三，**demo 模式可以无 key 运行**的这一点本身是设计特性，让"全新有人带 key 才能跑"变成"不绑 key 也能演示"；四，**本地跑通 ≠ 平台跑通**——Dockerfile 硬编码 `--port 8000`，部署 Render 后被平台的 `PORT` 环境变量捉住，我修掉并写进 README。这些只有交付才会暴露的现实约束，恰恰是最有价值的工程课。

## 方法论批判：Superpowers 的假设在我的项目里站得牢吗

Superpowers 至少假设三件事，在我的项目里只有部分成立。

A. 假设宿主会暴露可调用的 skill——我只暴露 1/6，该假设不成立。
B. 假设"spec 写清楚就足以自主推进"——第 1~6 轮证明，写清楚必须经过"一个只拿白纸、没有任何历史的新 agent 一步步读"才算数，而这仍要人主动发起，不是框架自动保证。
C. 假设"框架自动守纪律"——TDD 的红绿是我人工一次记录，纪律由 `git`、`pytest`、两阶段评审这些机制在守。

最有趣的是自指的那一条：Superpowers 自己也是一个 harness，它用权限、审批、记忆来"守纪律"；而按课程 §A.4 的判据，它的这些机制大部分是"内容物"（配置/提示词），真正强制还是要靠宿主 shell。它强调的"预防胜于提示"的理念，正好是我用来审视它"它能不能不守提示来保证?"这面镜子的解释力。没有代码的护栏，一切守卫都是内容物——这句话我可以套用在自己交付的 SafePatch 上，也可以套在 Superpowers 自身上。

## 最后的答案

SafePatch 不是"更会写代码的 agent"，而是一个能自证"机制=代码"的 harness：自己写的 agent 主循环、可注入的 mock provider、确定性护栏/审批/反馈闭环、SQLite 记忆、加密凭据与脱敏。它的成色不在界面，而在可验证性：`88 passed, 1 skipped` 的测试、`python -m safepatch.demo` 三种机制稳定复现，Docker 能构建起服务，线上 WebUI 可访问，凭据不回显明文。

如果重做，我仍然会做同样的治理型 harness，但会把"陌生人冷启动"更早移入日程（而不是在 mechanisms 完成后再补），并且第一天就接入远端 CI 与平台部署——第 4 轮的 spec 歧义和 `PORT` 教训都告诉我们，早暴露比晚暴露便宜得多。

课程真正的收获，是我学会把" 做与不做、对与错"写成文档闭环的程序，而不是押在 LLM 的自觉上。工具强不强，取决于人愿不愿意把"行与不行"明写成硬边界——那行返回 `denied` 的 `guardrail()`，就是我对"工程师的价值在哪里"的最终回答。